{
  "version": "1.0",
  "name": "mcp-default-policy",
  "description": "Default policy for a MCP server",
  "rules": [
    {
      "name": "unrestricted-access-api-key",
      "description": "All principals can list and execute tools, resources, and prompts",
      "effect": "allow",
      "principal_conditions": [
        {
          "path": "attributes.api_key",
          "operator": "in",
          "value": ["serdar"]
        }
      ],
      "resource_conditions": [],
      "actions": [
        "list","execute"]
    }
  ],
  "default_effect": "deny"
}